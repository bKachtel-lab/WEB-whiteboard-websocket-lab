{
  "name": "web-whiteboard-websocket-lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -s first \"node server.js\" vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "ws": "^8.18.1"
  }
}
